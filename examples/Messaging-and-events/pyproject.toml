[project]
name = "messaging-and-events"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.69",
]

[dependency-groups]
dev = [
    "boto3-stubs[events,sns,sqs]>=1.43.69",
]
